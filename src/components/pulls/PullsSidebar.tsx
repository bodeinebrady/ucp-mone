// Superseded by the shared AppSidebar (same labeled sidebar used on every
// screen). Kept as a thin re-export for any lingering imports.
export { AppSidebar as PullsSidebar } from "@/components/AppSidebar";
